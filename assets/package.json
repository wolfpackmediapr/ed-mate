{
  "name": "assets",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "sass sass/main.scss:css/main.css --style=compressed",
    "dev": "sass sass/main.scss:css/main.css --watch",
    "build:watch": "sass sass/main.scss:css/main.css --watch --style=compressed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Frontend assets build process for Edumate LMS",
  "devDependencies": {
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.10",
    "sass": "^1.89.2"
  }
}
